[Unit]
# Rate-limit restarts so a hard-crash loop doesn't spin forever.
StartLimitIntervalSec=60
StartLimitBurst=5

[Service]
# Waybar v0.15.0 crashes intermittently with SIGABRT (a module throwing).
# Always bring it back.
Restart=always
RestartSec=2
