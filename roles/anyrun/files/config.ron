Config(
    x: Fraction(0.5),
    y: Fraction(0.3),
    width: Absolute(800),
    height: Absolute(0),
    hide_icons: false,
    ignore_exclusive_zones: false,
    show_results_immediately: true,
    max_entries: Some(8),
    plugins: [
        SharedLibrary("libapplications.so"),
        SharedLibrary("libsymbols.so"),
        SharedLibrary("libtranslate.so"),
        SharedLibrary("libkeyboard.so"),
    ],
)
